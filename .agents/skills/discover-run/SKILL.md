# Discover run

Run `npx tsx src/cli/index.ts discover --goal "..." --mock` for CI.

Live: `OPENAI_API_KEY` + `--live-browser`. Custom tools only. Spend cap `DISCOVERY_MAX_USD`.

Write journals under `evidence/discovery/<runId>/`. Compile durable locators; drop ARIA refs.
