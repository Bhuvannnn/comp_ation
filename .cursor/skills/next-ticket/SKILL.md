---
description: Start the next sequential ticket
---

# Next ticket

1. Read `_lab/agent_ops/TICKETS.md`.
2. Do **only** the first ticket that is not `[x]`.
3. Follow `AGENTS.md` (no LLM in replay, no secrets, no queues).
4. Run `npm test` and `npm run typecheck`.
5. Stop. Summarize: what changed, how to run it, what ticket is next.

Do not start two tickets in one chat.
