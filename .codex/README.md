# Codex / Cursor skills mirror

Skills live in `.agents/skills/*/SKILL.md` (discover-run, replay-run, hitl-handoff, write-report, package-evidence).

Root `AGENTS.md` is the cross-tool contract. Optional `.codex/config.toml` is unused; keep the project trusted via AGENTS.md only.
